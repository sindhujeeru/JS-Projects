import axios from axios;
export default class Recipe{
    constructor(id){
        this.id = id;
    }
    async getRecipe(){
        try {
            
        } catch (error) {
            console.log(error);
        }
        
    }
}